/* SF Window Station — site configuration.
 * FORM SETUP (one-time): create a free form at https://formspree.io, then paste
 * its endpoint below, e.g. "https://formspree.io/f/xabcdefg".
 * Every form on the site (contact forms + estimate wizard) reads this value.
 * While it is empty, forms show a "call us" fallback instead of failing silently.
 */
window.WS_CONFIG = {
  formEndpoint: "", // e.g. "https://formspree.io/f/xabcdefg"
  phone: "415-295-4502",
  phoneHref: "tel:4152954502"
};
